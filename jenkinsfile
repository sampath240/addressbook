pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
              }
            }
        stage('Publish') {
            steps {
               nexusPublisher nexusInstanceId: 'admin', nexusRepositoryId: 'releases', packages: [[$class: 'MavenPackage', mavenAssetList: [[classifier: '', extension: '', filePath: '/var/lib/jenkins/workspace/addressbook1/addressbook_main/target/addressbook.war']], mavenCoordinate: [artifactId: 'addressbook.war', groupId: 'org.jenkins-ci.main', packaging: 'war', version: '2.23']]]
            }
        }
    } 
}
